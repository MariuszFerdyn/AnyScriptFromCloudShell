# AnyScriptFromCloudShell
This project downloads and runs scripts (Bash, Azure CLI, Python, or any other interpreter available in a container image) from network sources and executes them inside isolated, disposable containers. 
